<h1>My Lists</h1>
<div class="container">
    {{#each}}
        <div class="col-xs-4">
            <h2>{{title}}</h2>
            {{render 'cards' cards}}
            <button {{action 'createCard' this}}>MAKE SOMETHING MAGICAL</button>
        </div>
    {{/each}}
</div>
